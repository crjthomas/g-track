rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // User exports: users/{userId}/exports/{fileName}
    match /users/{userId}/exports/{fileName} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}

